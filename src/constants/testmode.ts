// Always-on test-mode marker shown at the very top of every screen while MyKeyz Care is
// pre-launch. BUMP this code on EVERY shippable change (app OR backend) so Moshe can tell at a
// glance which build/OTA he is looking at. Care keeps its own counter, independent of the main app.
export const TESTMODE_CODE = "00005";
